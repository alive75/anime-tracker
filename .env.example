# .env.example

# --- API & General ---
# A strong secret for signing JWT tokens.
# Generate one using: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=7d

# --- Database (PostgreSQL) ---
# This URL is used by Prisma to connect to the database.
# The format is: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://docker:docker@db:5432/anime_tracker"

# These variables are used by the 'postgres' Docker image to initialize the database.
POSTGRES_USER=docker
POSTGRES_PASSWORD=docker
POSTGRES_DB=anime_tracker

# --- Frontend (Web) ---
# Your Google Gemini API Key.
# This key will be baked into the web Docker image at build time.
API_KEY=your_gemini_api_key_here
